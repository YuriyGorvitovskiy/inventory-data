mutation {
    upsert_third(
        name: "Name8",
        indx: 8,
        third: {
            first: 2,
            second: "${0}",
            bool: true,
            time: "${1}"
        }
    ) {
        bool
    }
}
