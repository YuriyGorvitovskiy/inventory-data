query {
    second(filter: {id:["${0}", "${2}"]}, order: {int: ASC}) {
        id
        first
        one
        two
        third_name
        third_indx
        double
        int
        short
        long
    }
}
